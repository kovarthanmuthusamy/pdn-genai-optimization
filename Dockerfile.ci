FROM python:3.10-slim

WORKDIR /app

# Copy repo
COPY . .

# Install minimal deps (no CUDA, CPU torch only)
RUN pip install --no-cache-dir \
    torch==2.7.1 --index-url https://download.pytorch.org/whl/cpu \
    numpy pydantic pytest

# Smoke test
CMD ["python", "--version"]
