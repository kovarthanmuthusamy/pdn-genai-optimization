% ch1-introduction.tex -- part of thesis_v3.tex; do not add \documentclass here.
% Academic-register pass. Source-file references live in Appendix D (Implementation Index).
% Unsupported claims must use \fillin{...}.

\chapter{Introduction}
\label{ch:introduction}

\section{Motivation and Problem Statement}
\label{sec:motivation_problem}

A printed circuit board's power delivery network (PDN) has to supply the integrated circuits on
the board with a stable voltage while they draw current that changes quickly. In the frequency
domain this requirement is usually written as the target impedance rule: the impedance $Z(f)$ seen
looking into the PDN from the load must stay below a frequency-dependent limit
$Z_{\mathrm{target}}(f)$ across the band of interest \cite{smith1999target}. Decoupling capacitors,
commonly shortened to decaps, are the main tool the designer has for shaping $Z(f)$, and the design
task is to decide how many of them to place and where to put them.

The band of interest in this work is $1$--$600$\,MHz, sampled at $231$ discrete frequency bins
running from $1$\,MHz to $600$\,MHz. The grid is not uniformly spaced: the step between successive
bins takes the three values $0.1$\,MHz, $1$\,MHz and $10$\,MHz, so the low end of the band is
resolved more finely than the high end. The impedance requirement is a mask over the same $231$
bins, one limit per bin. It is not a constant. It rises from $0.8$ at the low end to $50$ at the
high end and varies with frequency in between; the values are read as ohms throughout this thesis.
\fillin{specify the impedance requirement and the frequency grid properly: (i) the provenance of
the mask -- who specified it and from which load-current or ripple budget it was derived; (ii)
confirmation of its unit, since the ohms reading is inferred from the naming used in the
optimization code rather than from a unit recorded with the mask itself; and (iii) why the
frequency sweep uses three different step sizes and who chose the breakpoints. The solver sweep
setup used to export the grid, together with a design note on the mask, would settle all three.}

The design under study exposes a fixed set of $52$ candidate decap sites. These are the valid cells
of a $7 \times 8$ physical grid, four of whose cells are marked invalid, at grid positions
$(0,3)$, $(0,4)$, $(6,3)$ and $(6,4)$; the remaining $52$ slots are labelled C1 to C52. A design
decision is therefore a binary placement vector
%
\begin{equation}
  \mathbf{b} \in \{0,1\}^{52},
  \qquad
  K = \lVert \mathbf{b} \rVert_0 ,
  \label{eq:placement_vector}
\end{equation}
%
where $b_i = 1$ means that a decap is mounted in slot $i$, and the budget $K$ is the number of
decaps used. $K$ matters because decaps cost money and board area, so a placement that meets the
mask with fewer parts is preferable to one that needs more.

Two properties make the problem hard. The map $\mathbf{b} \mapsto Z(\cdot)$ is available only
through an expensive field solver, so every candidate placement that is to be judged exactly has
to be simulated. And the discrete space has $2^{52}$ elements, far beyond what can be enumerated.
Neither exhaustive search nor a simulation-in-the-loop search that evaluates a large number of
candidates is practical.

The conventional workflow that this thesis targets is iterative. The engineer places a set of
decaps, simulates the resulting impedance spectrum, and checks it against the mask. If peaks
violate the mask, a spatial power-integrity distribution is computed at the offending frequency,
decaps are moved towards the hotspots that this distribution reveals, and the cycle repeats. Every
turn of that loop contains at least one full solver run.
\fillin{quantify the cost of this loop for the design under study -- engineer hours per iteration
and the number of iterations typically needed to converge. This has not been measured here, and no
freestanding study measuring it was found.}
That iterative and manual decap tuning is slow enough to motivate a learned replacement is asserted
in the motivation sections of the machine-learning and genetic-algorithm decap literature rather
than established by a dedicated measurement study
\cite{swaminathan2020demystifying,zhang2020enhanced,ars2023ai,convga2024gcn}.

Ground-truth impedance data for this work is produced with the ECADSTAR power-integrity flow, which
runs on a Windows host driven remotely from a Linux environment.
\fillin{report the measured cost of the ground-truth flow and of its learned replacement: the
wall-clock time of one power-integrity simulation, per layout and per simulation batch, and the
surrogate's inference time on the same hardware, from which the speed-up factor follows. A timing
log from a simulation batch and a benchmark run of the trained surrogate would settle both.}

Learning-based decap placement is not new. Deep reinforcement learning was applied to decap
selection from 2019 onwards \cite{zhang2019decoupling}, extended with an impedance-matrix
reduction method and double Q-learning to cut optimisation time \cite{zhang2020enhanced}, and
applied to silicon-interposer 2.5-D and 3-D IC decap design in the same period
\cite{park2020drldecap}. The same line later moved to transformer-based policies
\cite{park2022transformer,kim2023devformer}, while separate strands combine graph neural networks
with genetic algorithms \cite{convga2024gcn} or use learned models purely as fast impedance
predictors \cite{zhang2021fast}. Chapter~\ref{ch:theory_background} treats this body of work in
detail and states where the framework presented here differs from it.

The framework developed in this thesis replaces the conventional loop with four elements. A
learned, differentiable surrogate of one fixed board, built as a multi-input variational
autoencoder, stands in for the field solver. Gradient-based inverse design in the latent space of
that surrogate searches for placements under a budget $K$. An active-learning loop refines the
surrogate on layouts where its error or uncertainty is high, simulating only a selected batch
rather than every candidate. A performance-analysis layer then decodes a frequency-conditioned
spatial heatmap at the peak frequencies of a predicted spectrum, so that a predicted violation can
be inspected spatially and not only in the spectrum. Whether the decoded heatmaps are accurate
enough for that purpose is a question for Section~\ref{subsec:val_pi_heatmaps}; nothing in this
chapter asserts that they are.

The surrogate models three modalities of a single board jointly: the decap occupancy as the binary
vector of Equation~\eqref{eq:placement_vector}, the power-integrity spectrum magnitude over the
$231$ frequency bins, and a $64 \times 64$ spatial power-integrity heatmap at selected frequency
anchors in the megahertz range. The budget $K$ and the inspection frequency enter as conditioning
inputs. $K$ is the number of ones in the occupancy vector, so $K \in \{0,\dots,52\}$.

The inverse-design objective is stated per budget. For a given $K$, a placement is feasible if the
predicted spectrum stays below the mask with a safety margin $m$,
%
\begin{equation}
  \hat{Z}(f; \mathbf{b}) \;\le\; Z_{\mathrm{target}}(f) - m
  \qquad \text{for all } f \text{ in the } 231 \text{ bins},
  \label{eq:feasibility_intro}
\end{equation}
%
where $\hat{Z}$ denotes the surrogate's predicted impedance as opposed to the simulated $Z$. Among
the feasible candidates, the one with the lowest peak impedance $\max_f \hat{Z}(f;\mathbf{b})$ is
selected. If no candidate for a given $K$ is feasible, the procedure records that outcome
explicitly rather than returning a best-effort placement. The latent search is implemented against
an earlier surrogate, from experiment \texttt{exp038\_true\_multi}, and not against the current one,
\texttt{exp059\_capacity\_freq}. It has therefore not been run end to end on the surrogate described
in this thesis, and no inverse-design outcome is attributed to that surrogate anywhere in what
follows.

One detail of the search is load-bearing enough to introduce here, because it shapes the whole
formulation. Reading a placement out of the decoder requires a hard top-$K$ selection, which is a
discrete operation with no useful gradient. The straight-through operator used in the search
returns the hard top-$K$ binary vector in the forward pass but passes the gradient of the soft
occupancy probabilities backwards. Without it, the top-$K$ sort severs the gradient and the latent
vector never moves in response to the spectrum target.

The scope is deliberately narrow. The surrogate is trained for a single board and one design
family, and no cross-board generalisation is claimed. The decap type and the $52$-slot set are
fixed, so no multi-value or multi-type capacitor catalogue is covered, and the training data is
filtered to layouts with $K \le 30$. The normalised training dataset comprises $24979$ unique
layouts and $582997$ manifest rows.

Surrogate spectra and heatmaps are predictions, not sign-off. A proposed design has to be
re-simulated with the ground-truth power-integrity flow before it can be accepted. This boundary
is repeated here because it constrains every result claim made later in the thesis.

\fillin{describe the design under study -- layer stack, physical dimensions, IC port location and
the power bus being decoupled -- and state the capacitance value and part type assumed for the $52$
slots. The board is identified in the simulation setup only by a project name and a power-bus name,
and the one decap-profile artifact that exists belongs to a legacy experiment rather than to the
current surrogate; the original ECAD project and a board specification note would settle both.}

\fillin{personal and industrial motivation for choosing this topic -- to be written by hand.}

\figplace{Board slot map: the $7\times 8$ grid with the four invalid cells removed, slots labelled C1--C52.}

\figplace{Conventional place--simulate--inspect--adjust loop next to the proposed surrogate-based loop.}

\section{Research Objectives and Contributions}
\label{sec:objectives_contributions}

The overall aim follows from Section~\ref{sec:motivation_problem}: to replace the
simulate-in-the-loop decap placement search on one fixed board with a learned differentiable
surrogate, to perform the search by gradient descent in that surrogate's latent space under a
budget constraint, and to add an analysis layer that localises impedance violations spatially.
Active learning is included as a mechanism for spending a limited simulation budget on the layouts
where the surrogate is least reliable.

Beyond that aim, the research questions and the contribution list are not yet fixed, and both
require agreement with the supervisor before they are written down.

\fillin{RQ1--RQn: formulate the research questions with the supervisor. Candidate themes arising
from the work are (i) whether a multi-modal VAE surrogate of one board is accurate enough to drive
placement decisions, (ii) whether latent-space gradient search through a straight-through top-$K$
read-out finds feasible placements at smaller $K$ than the alternatives, and (iii) whether active
learning improves the surrogate where it matters under a fixed simulation budget. These are
candidates only and must not be treated as agreed.}

\fillin{Contribution list: enumerate the claimed contributions once the experiments in
Chapter~\ref{ch:results_evaluations} are complete and agreed with the supervisor. This list must
match Section~\ref{sec:summary_contributions} in the conclusion.}

\fillin{State explicitly which of the objectives are demonstrated end to end and which are not. The
latent search currently runs against an earlier surrogate rather than the one this thesis
documents, so no end-to-end inverse-design result may be claimed for the current surrogate until
the two stages are rewired. Resolve before claiming any objective as met.}

\fillin{Confirm the delimitation sentence: what this thesis does not attempt -- signal-integrity
sign-off, electromagnetic field solving and emissions, and transfer to other boards. Wording to be
agreed.}

\section{Structure of the Thesis}
\label{sec:thesis_structure}

The thesis has five chapters. The front matter provides a table of contents, a list of figures, a
list of tables, an unnumbered chapter on mathematical notation and an unnumbered list of acronyms.

Chapter~\ref{ch:theory_background}, \emph{Theoretical Background}, sets out the theory the rest of
the work rests on. Section~\ref{sec:pdn_optimization} covers power delivery networks and the
optimisation problem, with subsections on impedance and decoupling
(\ref{subsec:impedance_decoupling}), power-integrity simulation and its multimodal outputs
(\ref{subsec:pi_simulation_multimodal}), and the formal decap placement formulation
(\ref{subsec:decap_placement_formulation}). Section~\ref{sec:generative_latent_opt} covers
generative modelling and latent-space optimisation, with subsections on multimodal variational
autoencoders (\ref{subsec:vae_multimodal}), physics surrogates (\ref{subsec:physics_surrogate})
and latent-space optimisation (\ref{subsec:latent_space_opt}).
Section~\ref{sec:related_work} reviews related work on PDN optimisation methods
(\ref{subsec:pdn_opt_methods}) and generative learning approaches
(\ref{subsec:generative_learning_approaches}) and closes with the research gap
(\ref{subsec:research_gap}).

Chapter~\ref{ch:design_implementation}, \emph{Design and Implementation}, describes what was built.
Section~\ref{sec:data_prep_reprocessing} covers data preparation and reprocessing: the raw
power-integrity outputs (\ref{subsec:raw_pi_outputs}), the multi-frequency dataset
(\ref{subsec:multifreq_dataset}) and the splitting and sampling strategy
(\ref{subsec:data_splitting_sampling}). Section~\ref{sec:multimodal_vae_surrogate} describes the
multi-modal variational autoencoder used as the generative surrogate, covering the architectural
design (\ref{subsec:architectural_design}), the training objective and physics regularisation
(\ref{subsec:objective_physics_reg}), training and inference (\ref{subsec:training_inference}) and
the auxiliary guidance network (\ref{subsec:aux_guidance_network}).
Section~\ref{sec:latent_opt_decap} formulates the latent-space optimisation of decap placement
(\ref{subsec:opt_problem_formulation}) and the optimisation procedure with its feasibility test
(\ref{subsec:opt_procedure_feasibility}). Section~\ref{sec:experimental_setup} states the
experimental setup and Section~\ref{sec:active_learning} the active-learning loop used to scale the
model.

Chapter~\ref{ch:results_evaluations}, \emph{Results and Evaluations}, reports the measurements.
Section~\ref{sec:surrogate_performance} evaluates the generative surrogate through reconstruction
performance (\ref{subsec:reconstruction_perf}), validation of the predicted power-integrity
heatmaps (\ref{subsec:val_pi_heatmaps}) and evaluation of the auxiliary guidance network
(\ref{subsec:eval_aux_guidance}). Section~\ref{sec:latent_design_opt_results} reports the
latent-space design optimisation results, covering scalability and efficiency
(\ref{subsec:eval_scalability_efficiency}) and the spatial and capacitor-type evaluation
(\ref{subsec:spatial_cap_type_eval}). Section~\ref{sec:limitations} states the limitations and
Section~\ref{sec:ablations} the ablations.

Chapter~\ref{ch:conclusion_outlook}, \emph{Conclusion and Outlook}, summarises the contributions
(\ref{sec:summary_contributions}) and gives directions for future research
(\ref{sec:future_research}).

Four appendices follow the bibliography. Appendix~\ref{ch:appendix_simulation_dataset} documents
the power-integrity simulation environment and the dataset specification.
Appendix~\ref{ch:appendix_hyperparameters} lists the hyperparameter configuration used for training
and optimisation. Appendix~\ref{ch:appendix_supp_results} collects supplementary experimental
results. Appendix~\ref{ch:appendix_implementation_index} is an implementation index: it records,
section by section, the mapping from statements in the running text to the project artifacts they
were taken from, so that the chapters can be read as continuous prose without losing traceability.
\fillin{confirm what Appendix C will contain; its scope depends on experiments not yet run.}

% ---------------------------------------------------------------
% FILL IN checklist:
%   1. Impedance requirement and frequency grid (MERGED marker, was three separate markers in the
%      pre-register draft: mask provenance, mask unit, grid step sizes). Resolved by (a) a design
%      note stating who specified the mask and from which current/ripple budget, (b) a unit recorded
%      with the mask -- the ohms reading is inferred from the identifiers imp_ohm / max_ohm in the
%      optimization script, (c) the solver sweep setup that produced the 231-bin grid.
%   2. Cost of the conventional place-simulate-inspect-adjust loop (engineer hours, iteration
%      count). Not measured here; no freestanding paper measures it. Needs an industrial source or
%      removal of the quantitative framing.
%   3. Timing (MERGED, was two markers: solver wall-clock, surrogate inference time). Resolved by a
%      timing log from a simulation batch plus a benchmark run of the trained surrogate on stated
%      hardware. Only configured timeouts exist today (18000 s batch wait, 900 s batch start, 40 s
%      poll) -- deliberately dropped from the body, since they are configuration, not measurement.
%   4. Board specification and decap part (MERGED, was two markers). Board is identified in the
%      simulation configuration only by an ERF project name and the powerbus name; the decap-profile
%      artifact belongs to a legacy experiment, not the current track.
%   5. Personal/industrial motivation -- hand-written by Kovarthanam.
%   6. RQ1--RQn -- require supervisor agreement. Candidate themes in the marker are suggestions only.
%   7. Contribution list -- must match sec:summary_contributions once written.
%   8. Which objectives are demonstrated end to end -- blocked by the Stage-2 wiring issue
%      (evidence warning 2 below).
%   9. Delimitation sentence wording.
%  10. Appendix C contents -- depends on experiments not yet run.
%
%   Markers retired in this pass, with where the gap now lives:
%   - Per-cycle active-learning budget conflict (config vs docs/active-learning.md): removed from
%     Section 1.3, which should describe chapter contents rather than carry a Chapter 3 evidence
%     conflict. The conflict is recorded in the implementation index and must be resolved in
%     Chapter 3 before either number is quoted. See evidence warning 4.
%   - Return signature of the current surrogate's forward pass and the form of its occupancy output
%     (logits vs. binarised): dissolved here because Chapter 1 no longer quotes encoder tensor
%     shapes. It is a Chapter 3 question and is recorded in the implementation index.
%
% Citation verification queue:
%   - smith1999target : used for the target-impedance rule. A prior audit found the stored DOI
%     pointed at a different 1999 paper; the refs.bib entry was repaired in a separate pass to DOI
%     10.1109/6040.784476, pages 284--291, checked against Crossref. The key is unchanged. Re-read
%     refs.bib before submission to confirm the repair is present. Other entries still carry the
%     generic "VERIFY: bibliographic fields not confirmed from source page" note; count them against
%     the current file rather than trusting any figure quoted here.
%   - zhang2019decoupling : verified = "partial". IEEE Xplore returned 403; title/authors/venue
%     confirmed from a co-author's publication list, page numbers unconfirmed.
%   - zhang2020enhanced, park2020drldecap : verified = "yes".
%   - swaminathan2020demystifying, ars2023ai, convga2024gcn, park2022transformer, kim2023devformer,
%     zhang2021fast : bibliographic fields not re-verified in this pass.
%   - The group {swaminathan2020demystifying, zhang2020enhanced, ars2023ai, convga2024gcn} supports
%     only the assertion that iterative decap tuning is slow, made in those papers' motivation
%     sections. It is NOT a measured benchmark and must not be paraphrased as one.
%
% Evidence warnings:
%   1. Current model track: the handoff notes and docs/README.md name exp059_capacity_freq as
%      current, while framework-overview.md and model-architecture.md still name
%      exp057_structured_graph. This chapter follows exp059 per the constitution. The root README
%      was not used as a source.
%   2. The latent optimization script loads exp038_true_multi checkpoints. No end-to-end
%      inverse-design result may be attributed to exp059. This chapter states the Stage-2 objective
%      and the straight-through top-K operator as method only, and says so in the rendered body at
%      the end of the objective paragraph in Section 1.1.
%   3. The performance-analysis (heatmap) layer is described as intent, not as demonstrated
%      capability; heatmap accuracy is deferred to subsec:val_pi_heatmaps.
%   4. AL per-cycle budget conflict between exp059_gp_error.json and docs/active-learning.md
%      remains unresolved; it is no longer surfaced in Chapter 1 (see retired markers above).
%   5. exp059 capacity-phase modality probabilities conflict (handoff notes say
%      layout_train_prob = 0 and occ_only_encode_prob = 0; config.yaml holds 0.65 and 0.4). Not used
%      in Chapter 1; must be resolved before Chapter 3 quotes either.
%   6. Chapter count conflict: the vault outline organises the material into at least 7 chapters;
%      thesis_v3.tex has 5 and is authoritative. Section 1.3 follows thesis_v3.tex.
%   7. Dataset anomaly, relevant later but not mentioned here: samples_per_mhz = 8480 for the
%      80 MHz anchor while all other anchors have 24979.
%   8. No experimental outcome is stated anywhere in this chapter, by design.
%   9. Parameter count for exp059 is unknown; the ~10.7M figure in the architecture documentation
%      belongs to exp057 and was deliberately not carried over.
%  10. Section 1.3 now names four appendices, matching thesis_v3.tex after Appendix D
%      (Implementation Index) was added. All source-file references removed from this chapter in
%      the register pass are listed in scratchpad thesis_phase1/ch1-impl-index.json and belong in
%      Appendix D.
% ---------------------------------------------------------------
